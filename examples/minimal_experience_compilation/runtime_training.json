{
  "runtime_training_id": "runtime-training-example-001",
  "source_compilation_id": "example-minimal-001",
  "title": "Structured note and JSON summary workflow",
  "confidence": "high",
  "minimal_prompt_context": "Create the markdown note first, create the JSON summary second, then verify that both files exist and that the JSON includes title, summary, next_steps, and status.",
  "output_contract": {
    "required_files": ["planning_note.md", "summary.json"],
    "json_fields": ["title", "summary", "next_steps", "status"]
  },
  "artifact_contract": {
    "required_artifacts": [
      { "path": "planning_note.md", "type": "markdown" },
      { "path": "summary.json", "type": "json" }
    ]
  },
  "prefinal_verification_checklist": [
    {
      "check_id": "required_files_exist",
      "description": "Verify both required files exist before finishing.",
      "verification_method": "file_exists",
      "blocking": true
    }
  ],
  "source_evidence_refs": ["evidence:trace-step-001", "evidence:trace-step-002"]
}
